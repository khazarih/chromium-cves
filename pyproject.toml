[project]
name = "chromium-cves"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.2.2",
    "chromadb>=1.0.0",
    "requests>=2.32.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]
